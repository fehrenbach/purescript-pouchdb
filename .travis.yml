language: node_js
node_js: node
dist: trusty
sudo: false
install:
  - npm install -g bower purescript pulp
script:
  - npm install pouchdb
  - bower install
  - pulp test
after_success:
  - test $TRAVIS_TAG &&
    echo $GITHUB_TOKEN | pulp login &&
    echo y | pulp publish --no-push
